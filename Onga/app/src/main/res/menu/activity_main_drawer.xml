<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_menu" android:icon="@android:drawable/ic_menu_camera"
            android:title="@string/app_drawer_menu" />
        <item android:id="@+id/nav_history" android:icon="@drawable/ic_history"
            android:title="@string/app_drawer_history" />
        <item android:id="@+id/nav_account" android:icon="@android:drawable/ic_menu_manage"
            android:title="Accounts" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/nav_logout" android:icon="@android:drawable/ic_menu_call"
                android:title="@string/app_drawer_logout" />
            <item android:id="@+id/nav_about" android:icon="@android:drawable/ic_menu_help"
                android:title="@string/app_drawer_feedback" />
        </menu>
    </item>

</menu>